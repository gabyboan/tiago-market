# Rescate de copias locales — 18/09/2026

Carpeta principal: `tiago-market`, con Flutter en `apps/flutter_app/`.

## Conservado

- Configuraciones `dart_defines.local.json` y `android/app/google-services.json` restauradas en Flutter y excluidas de Git.
- 25 SQL de la copia Flutter y 11 de la antigua rama local `main`, archivados en `supabase/recovered/2026-09-18/` fuera de los directorios de migraciones activas.
- Reporte SQL de cobertura restaurado en `apps/flutter_app/tools/ingestion/sql/`.
- Dos archivos privados `.tar.gz` bajo `.local-rescue/2026-09-18/`: 258 y 73 archivos/links, respectivamente. Conservan código, configuraciones, herramientas de agentes y objetos Git residuales.
- Manifiestos SHA-256 y bundle de todas las referencias Git locales anteriores a la consolidación en la misma carpeta privada.

Cada archivo archivado se comparó con su original antes de borrar las copias. Se excluyeron compilaciones, dependencias, entornos Python y cachés regenerables. Los archivos privados no se publican en GitHub: es necesario copiarlos a otro almacenamiento privado para contar con respaldo fuera de esta computadora.

## Eliminado

- Copia `Documentos/proyectos/Tiago-Market-Flutter-App`.
- Copia incompleta `PROYECTOS/Tiago-Market/Tiago-Market`.

Espacio eliminado: 1.657.000.000 bytes de archivos asignados aproximadamente (1,66 GB decimales), menos el pequeño respaldo conservado.

No se borró el proyecto principal, no se reemplazó su código por versiones antiguas y no se modificó la base de datos remota.

## Recuperación local

Abrir los `.tar.gz` con un gestor de archivos o extraerlos en una carpeta temporal vacía. El bundle se puede inspeccionar con `git bundle list-heads` y clonar a otra carpeta. No publicar el contenido de `.local-rescue/`: incluye configuración privada.
